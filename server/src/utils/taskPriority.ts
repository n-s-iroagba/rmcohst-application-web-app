// server/src/utils/taskPriority.ts
// This seems unrelated to the current application. If it's an erroneous import, remove it.
// If it's genuinely needed:
export const calculateTaskPriority = (taskDetails: any): string => {
  console.log("Calculating task priority for:", taskDetails)
  return "HIGH" // Placeholder
}
